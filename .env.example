# Convex Configuration
CONVEX_DEPLOYMENT=your-deployment-name

# OpenAI Configuration (required for AI scoring and feedback)
CONVEX_OPENAI_API_KEY=your-openai-api-key-here
CONVEX_OPENAI_BASE_URL=https://api.openai.com/v1

# Note: Without OpenAI configuration, the app will use fallback scoring
# which provides basic scores based on answer length and completeness.
